<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title></title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">BOOK COOK</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addRecipe">Add new recipe</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update recipe</h2></div>
        <br>
        <div class="text-center">
          <% if (recipe) { %> 
            <form action="/editRecipe/<%= recipe.recipeId %>" method="POST">
                <label for="name">Name:</label><br>
                <input type="text" id="name" name="name" value = <%= recipe.name %> required><br><br>

                <label for="ingredients">ingredients:</label><br>
                <input type="text" id="ingredients" name="ingredients" value = <%= recipe.quantity %> required><br><br>

                <label for="instructions">instructions:</label><br>
                <input type="text" id="instructions" name="instructions" value = <%= recipe.price %> required><br><br>

                <label for="image">Image:</label><br>
                <input type = "text" name = "currentImage" value = <%= recipe.image %> disabled><br>
                <img src = "/images/<%= recipe.image %>" width="20%"><br><br>

                <button type="submit">Update recipe</button>
            </form>
          <% } else { %>
              <p>No recipe found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
