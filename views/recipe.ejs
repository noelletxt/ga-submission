<!-- recipe.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>BOOK COOK</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">BOOK COOK</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/recipe">Add new recipe</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>
    
    
    <div class="container">
        <br>
        <div class="text-center"><h2>recipe Details</h2></div>
        <br>
        <% if (recipe) { %> <!-- Check if recipe data exists -->
            <div>
                <p><b>Name:</b><br> <%= recipe.recipeName %></p>
                <p><b>Image:</b><br> <img src = "<%= recipe.image %>" width="20%"></p>
                <p><b>ingredients:</b><br> <%= recipe.quantity %></p>
                <p><b>instructions:</b><br> $<%= recipe.price.toFixed(2) %></p> 
            </div>
        <% } else { %>
            <p>No recipe found.</p>
        <% } %>
        <a href ="/">Back</a>
    </div>
    
</body>
</html>
